# Web game

TypeScript source for the browser version of AI Hype Tycoon (TanStack Start + React).
The native Play build lives in `/android`.

This folder is the game engine, UI, and HQ art — not a standalone install of the App Builder sandbox.
